# Copiá este archivo a .env y reemplazá los valores.

# Lista de hashes SHA-256 separados por coma. Cada hash representa una clave válida.
# Ejemplo para generar un hash: node -e "console.log(require('crypto').createHash('sha256').update('tu_clave').digest('hex'))"
VITE_ALLOWED_PASS_HASHES=8c5bc1cfcd4436b2c8bee2b8bf0a162f135a4ef45ff25f3ace1df11f726d2892

# Texto opcional que se muestra como pista en el formulario de login.
VITE_LOGIN_HINT=
